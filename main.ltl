// ltl-loki-push/main.ltl
//
// Grafana Loki push-API client for log shipping.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-loki-push v0.1.0")
    io::println("Grafana Loki push-API client for log shipping.")
    0
}
