// examples/demo.ltl — runnable demo for ltl_loki_push

import std::io
use ltl_loki_push as lib

fn main() {
    io::println("ltl_loki_push demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}